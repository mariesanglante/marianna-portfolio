import type { Metadata } from 'next';
import Prototype from './prototype';
export const metadata: Metadata = { title: 'Causal Brain | Weather Intelligence', description: 'Interactive aviation weather forecast prototype.' };
export default function Page() { return <Prototype />; }
