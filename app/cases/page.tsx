import cases from './data.json';
import {CaseHeader,CaseFooter} from './components';
export const metadata={title:'All cases — Marianna Gonchar',description:'Product design, systems, websites and teaching by senior product designer Marianna Gonchar.'};
const groups=[['Product','Product thinking, in practice.'],['Systems','Foundations that connect the details.'],['Web','A wider range of digital craft.'],['Teaching','Sharing the practice.'],['Research','Questions still being explored.']];
export default function CaseArchive(){return <><CaseHeader/><main id="main" className="wrap archive"><div className="archive-intro"><div className="eyebrow">THE WORK / {String(cases.length).padStart(2,'0')} PROJECTS & PERSPECTIVES</div><h1>Different challenges.<br/>The same <span className="serif">curiosity.</span></h1><p>From product direction to the last interaction.<br/>A closer look at my decisions, contributions, and craft.</p></div><a href="/cases/cloudbilling" className="cb-archive-feature"><div><span className="eyebrow">LATEST WORK · 2026 · LAUNCHED</span><h2>CloudBilling.<br/><i className="serif">Complex billing, clearly explained.</i></h2><span className="text-link">NDA · Unlock case study ↗</span></div><img src="/images/cloudbilling/section.png" alt="CloudBilling website revamp" width="1360" height="894"/></a><nav className="archive-nav" aria-label="Case categories">{groups.map(([key])=><a key={key} href={`#${key.toLowerCase()}`}>{key} <span>{cases.filter(c=>c.category===key).length}</span></a>)}</nav>{groups.map(([key,title])=><section className="archive-group" id={key.toLowerCase()} key={key}><div className="section-heading"><h2>{key}</h2><span>{title}</span></div><div className="case-list">{cases.filter(c=>c.category===key).map(c=><a href={`/cases/${c.slug}`} className={`case-row tone-${c.accent}`} key={c.slug}><div className="case-number">{String(cases.indexOf(c)+1).padStart(2,'0')}</div><div><div className="case-row-meta">{c.name}<span className="case-badge">NDA · Code required</span>{c.status&&<span className="case-badge">{c.status}</span>}</div><h3>{c.title}</h3><p>{c.summary}</p></div><span className="case-row-arrow" aria-hidden="true">↗</span></a>)}</div></section>)}</main><CaseFooter/></>}
