'use client';
import {useEffect,useRef,useState,type ReactNode} from 'react';
import {Pause,Play} from 'lucide-react';
export function LumioEditorialMotion({children}:{children:ReactNode}){const root=useRef<HTMLDivElement>(null);const [motion,setMotion]=useState(false);useEffect(()=>{const media=matchMedia('(prefers-reduced-motion: reduce)');const change=()=>setMotion(!media.matches);change();media.addEventListener('change',change);return()=>media.removeEventListener('change',change);},[]);useEffect(()=>{const nodes=root.current?.querySelectorAll('.le-reveal');if(!nodes)return;const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('le-visible');observer.unobserve(entry.target);}}),{threshold:.08});nodes.forEach(n=>observer.observe(n));return()=>observer.disconnect();},[]);return <div ref={root} className={`le-case ${motion?'le-motion':'le-still'}`}>{children}<button className="le-motion-control" onClick={()=>setMotion(v=>!v)} title={motion?'Pause case animations':'Enable case animations'} aria-label={motion?'Pause case animations':'Enable case animations'}>{motion?<Pause size={16}/>:<Play size={16}/>}</button></div>;}
